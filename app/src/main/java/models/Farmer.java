package models;

public class Farmer extends User {

    public Farmer(String firstName, String lastName, String username, String password) {
        super(firstName, lastName, username, password);
    }
}
